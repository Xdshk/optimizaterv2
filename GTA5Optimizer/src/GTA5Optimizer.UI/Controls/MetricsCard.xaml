<UserControl x:Class="GTA5Optimizer.UI.Controls.MetricsCard"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#1e1e1e" BorderBrush="#333333" BorderThickness="1" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding Title}" Foreground="#888888" FontSize="12"/>
            <TextBlock Grid.Row="1" Text="{Binding Value}" Foreground="White" FontSize="24" FontWeight="Bold"/>
            <ProgressBar Grid.Row="2" Height="4" Margin="0,10,0,0"
                         Value="{Binding Percentage}" Maximum="100"
                         Foreground="#00d4ff"/>
        </Grid>
    </Border>
</UserControl>