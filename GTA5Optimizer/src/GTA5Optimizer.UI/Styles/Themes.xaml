<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ==================== ЦВЕТОВАЯ ПАЛИТРА ==================== -->
    <Color x:Key="BgPrimary">#0D0D0D</Color>
    <Color x:Key="BgSecondary">#161616</Color>
    <Color x:Key="BgCard">#1A1A1A</Color>
    <Color x:Key="BgCardHover">#222222</Color>
    <Color x:Key="BgElevated">#1E1E1E</Color>
    <Color x:Key="BorderPrimary">#282828</Color>
    <Color x:Key="BorderSecondary">#333333</Color>
    <Color x:Key="TextPrimary">#FFFFFF</Color>
    <Color x:Key="TextSecondary">#808080</Color>
    <Color x:Key="TextMuted">#555555</Color>
    <Color x:Key="AccentCyan">#00D4FF</Color>
    <Color x:Key="AccentCyanDark">#0099BB</Color>
    <Color x:Key="AccentGreen">#00E676</Color>
    <Color x:Key="AccentAmber">#FFAB00</Color>
    <Color x:Key="AccentRed">#FF5252</Color>
    <Color x:Key="AccentOrange">#FF6D00</Color>
    <Color x:Key="AccentPurple">#B388FF</Color>

    <SolidColorBrush x:Key="BgPrimary" Color="{StaticResource BgPrimary}"/>
    <SolidColorBrush x:Key="BgSecondary" Color="{StaticResource BgSecondary}"/>
    <SolidColorBrush x:Key="BgCard" Color="{StaticResource BgCard}"/>
    <SolidColorBrush x:Key="BgCardHover" Color="{StaticResource BgCardHover}"/>
    <SolidColorBrush x:Key="BgElevated" Color="{StaticResource BgElevated}"/>
    <SolidColorBrush x:Key="BorderPrimary" Color="{StaticResource BorderPrimary}"/>
    <SolidColorBrush x:Key="BorderSecondary" Color="{StaticResource BorderSecondary}"/>
    <SolidColorBrush x:Key="TextPrimary" Color="{StaticResource TextPrimary}"/>
    <SolidColorBrush x:Key="TextSecondary" Color="{StaticResource TextSecondary}"/>
    <SolidColorBrush x:Key="TextMuted" Color="{StaticResource TextMuted}"/>
    <SolidColorBrush x:Key="AccentCyan" Color="{StaticResource AccentCyan}"/>
    <SolidColorBrush x:Key="AccentCyanDark" Color="{StaticResource AccentCyanDark}"/>
    <SolidColorBrush x:Key="AccentGreen" Color="{StaticResource AccentGreen}"/>
    <SolidColorBrush x:Key="AccentAmber" Color="{StaticResource AccentAmber}"/>
    <SolidColorBrush x:Key="AccentRed" Color="{StaticResource AccentRed}"/>
    <SolidColorBrush x:Key="AccentOrange" Color="{StaticResource AccentOrange}"/>
    <SolidColorBrush x:Key="AccentPurple" Color="{StaticResource AccentPurple}"/>

    <!-- ==================== ГРАДИЕНТЫ ==================== -->
    <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#00D4FF" Offset="0"/>
        <GradientStop Color="#00E5FF" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="CardGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#1A1A1A" Offset="0"/>
        <GradientStop Color="#141414" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="OptimizeButtonGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#00D4FF" Offset="0"/>
        <GradientStop Color="#0091EA" Offset="1"/>
    </LinearGradientBrush>

    <!-- ==================== АНИМАЦИИ ==================== -->
    <Storyboard x:Key="FadeIn">
        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="FadeInUp">
        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="12" To="0" Duration="0:0:0.4">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="PulseGlow">
        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.5" To="1" Duration="0:0:1.5" AutoReverse="True" RepeatBehavior="Forever">
            <DoubleAnimation.EasingFunction>
                <SineEase EasingMode="EaseInOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="ButtonClickBounce">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.X)" From="1" To="0.95" Duration="0:0:0.08" AutoReverse="True"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.Y)" From="1" To="0.95" Duration="0:0:0.08" AutoReverse="True"/>
    </Storyboard>

    <!-- ==================== СТИЛЬ КНОПКИ: ОСНОВНАЯ ==================== -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource OptimizeButtonGradient}"/>
        <Setter Property="Foreground" Value="#000000"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="24,12"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#00D4FF" Opacity="0.3" ShadowDepth="0" BlurRadius="15"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#33E0FF" Offset="0"/>
                                        <GradientStop Color="#00ABE0" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#00D4FF" Opacity="0.5" ShadowDepth="0" BlurRadius="20"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== СТИЛЬ КНОПКИ: ВТОРОСТЕПЕННАЯ ==================== -->
    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#252525"/>
        <Setter Property="Foreground" Value="#CCCCCC"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="18,10"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#333333"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#303030"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#444444"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#1A1A1A"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== СТИЛЬ ТАБОВ ==================== -->
    <Style x:Key="ModernTabControl" TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource BgPrimary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <Style x:Key="ModernTabItem" TargetType="TabItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Padding" Value="20,14"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid Background="Transparent">
                        <Border x:Name="border"
                                Background="Transparent"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#555555"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentCyan}"/>
                            <Setter Property="Foreground" Value="{StaticResource AccentCyan}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== СТИЛЬ PROGRESSBAR ==================== -->
    <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
        <Setter Property="Height" Value="3"/>
        <Setter Property="Background" Value="#1A1A1A"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border Background="{TemplateBinding Background}" CornerRadius="2"/>
                        <Border x:Name="PART_Track" CornerRadius="2">
                            <Border x:Name="PART_Indicator"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="2"
                                    HorizontalAlignment="Left"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== СТИЛЬ SCROLLVIEWER ==================== -->
    <Style x:Key="ModernScrollViewer" TargetType="ScrollViewer">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid>
                        <ScrollContentPresenter/>
                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                   Orientation="Vertical"
                                   HorizontalAlignment="Right"
                                   Width="6"
                                   Margin="0"
                                   Opacity="0.3"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== СТИЛЬ LISTVIEW ==================== -->
    <Style x:Key="ModernListView" TargetType="ListView">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
    </Style>

</ResourceDictionary>
